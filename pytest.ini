[pytest]
# 测试文件所在目录喵
testpaths = tests
# 让 pytest-asyncio 自动识别 async 测试函数，不用每个都手动加 mark 喵
asyncio_mode = auto
# 显式指定异步 fixture 的事件循环作用域，不写这行 pytest-asyncio 会一直警告喵
asyncio_default_fixture_loop_scope = function
# 输出简洁一点，只显示每个测试的结果和失败详情喵
addopts = -q --tb=short
